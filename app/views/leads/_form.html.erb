<%= simple_form_for(@lead) do |f| %>
  <%= f.input :name %>
  <%= f.input :surname %>
  <%= f.input :telephone %>
  <%= f.input :email %>
  <%= f.input :status, as: :radio_buttons, collection: ["Prospecto", "Interesado", "Cliente"] %>
  <%= f.submit %>
<% end %>
